package com.apinote.model.repository;

import com.apinote.model.Nota;
import org.springframework.data.jpa.repository.Query;
import org.springframework.stereotype.Repository;

import java.util.List;


public interface NotaRepository {

//    @Query("insert into nota (titulo_nota, descricao_nota) values (:titulo, :descricao) where id_usuario = :id")
//    void criarNota(String titulo, String descricao);
//
//    @Query("delete from nota where id_nota = :id")
//    void deletarNota(Long id);
//
//    @Query("update nota set titulo_nota = :titulo, descricao_nota = :descricao where id_nota = :id")
//    void atualizarNota(Long id, String titulo, String descricao);
//
//    @Query("select * from nota where id_nota = :id")
//    List<Nota> listarNotas();

}
